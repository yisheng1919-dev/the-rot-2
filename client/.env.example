# URL of your deployed THE ROT server (Socket.IO endpoint).
# For local dev, the default in src/socket.js (http://localhost:4000) is fine
# and you don't need this file at all.
VITE_SERVER_URL=https://your-server.onrender.com
